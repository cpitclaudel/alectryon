[latex writers]
documentoptions: a4paper
latex-preamble:
  % Keep Docutils defaults
  \usepackage{mathptmx} % Times
  \usepackage[scaled=.90]{helvet}
  \usepackage{courier}
  % Add geometry
  \usepackage{geometry}
